services:
  web:
    build:
      context: .
      args:
        NEXT_PUBLIC_APP_URL: http://localhost:3000
    environment:
      NEXT_PUBLIC_APP_URL: http://localhost:3000
      LOG_LEVEL: info
    ports:
      - "3000:3000"
    init: true
    restart: unless-stopped
