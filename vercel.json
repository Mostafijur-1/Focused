{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @focused/web build",
  "outputDirectory": "apps/web/.next",
  "regions": ["sin1"]
}
